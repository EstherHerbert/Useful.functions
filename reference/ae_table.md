# Produce a data frame to summarise count data.

**\[deprecated\]**

This function has been renamed to
[`count_table()`](https://estherherbert.github.io/Useful.functions/reference/count_table.md)
to reflect the fact that it can be used to summarise counts of any
events.

## Usage

``` r
ae_table(...)
```
